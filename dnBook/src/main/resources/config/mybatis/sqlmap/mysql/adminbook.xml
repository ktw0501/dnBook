<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dnBook.mapper.AdminBookMapper">

	<insert id="insertBook" parameterMap="book">
		insert 	into t_book 
		values	(#{bookCode},시퀀스(글번호),#{coverImg},#{title},#{publisher},#{author},
				#{translator},#{description},#{price},#{rentPrice},#{rentalDirPath},
				#{pdfPath},#{pubDate},#{categoryCode})
	</insert>
	<update id="updateBook" parameterType="book">
		update 	t_book
		set		cover_img = #{coverImg}, title = #{title},publisher = #{publisher},
				author = #{author}, translator = #{translator},
				description = #{description}, price = #{price},
				rent_price = #{rentPrice}, rental_dir_path = #{rentalDirPath},
				pdf_path = #{pdfPath}, pub_date = #{pubDate},
				category_code = #{categoryCode} 
		where	book_code = #{BookCode}
	</update>
	<delete id="deleteBook" parameterType="string">
		delete
		from	t_book
		where	#{bookCode}
	</delete>
	<select id="selectBook" resultType="book">
		select	book_code as bookCode, board_no as boardNo, cover_img as coverImg,
				title, publisher, author, translator, price, rental_price as rentalPrice,
				pub_date as pubDate, cetegory_code as categoryCode
		from	t_book
	</select>
	<delete id="deleteReview" parameterType="String">
		delete
		from	t_review
		where	#{bookCode}
	</delete>
	<delete id="deleteRecom" parameterType="int">
		delete
		from	t_recom
		where	#{recomNo}
	</delete>
    
</mapper>


























